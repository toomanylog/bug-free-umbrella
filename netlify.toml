[build]
  command = "npm run build"
  publish = "build"

[build.environment]
  CI = "false"
  SKIP_PREFLIGHT_CHECK = "true"

# Redirection pour le routage SPA
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200 